[project]
name = "c-swm"
version = "0.1.0"
description = "An implementation of the Contrastively-trained Structured World Model architecture"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
